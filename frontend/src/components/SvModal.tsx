import { type Servant } from "~/types"

interface ISvModalProps {
  servants?: Servant[]
  isShow: boolean
}

const SvModal = ({servants, isShow}: ISvModalProps) => {
  return <div></div>
}

export default SvModal;